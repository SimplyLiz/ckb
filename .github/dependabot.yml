version: 2
updates:
  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "develop"
    schedule:
      interval: "monthly"
    groups:
      actions:
        patterns:
          - "*"
    commit-message:
      prefix: "ci(deps)"

  # Go modules
  - package-ecosystem: "gomod"
    directory: "/"
    target-branch: "develop"
    schedule:
      interval: "monthly"
    groups:
      go-deps:
        patterns:
          - "*"
    commit-message:
      prefix: "fix(deps)"
    ignore:
      # Ignore patch updates for indirect deps to reduce noise
      - dependency-name: "*"
        update-types: ["version-update:semver-patch"]

  # npm (for the npm distribution wrapper)
  - package-ecosystem: "npm"
    directory: "/npm"
    target-branch: "develop"
    schedule:
      interval: "monthly"
    groups:
      npm-deps:
        patterns:
          - "*"
    commit-message:
      prefix: "fix(deps)"
